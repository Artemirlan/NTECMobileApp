<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ntecmobileapp.com.at.ntecmobileapp">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/FullscreenTheme" />
        <activity android:name=".LoginScreenActivity" />
        <activity
            android:name=".Main2Activity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".WelcomeMessageActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".SampleSlideFragment"
            android:theme="@style/FullscreenTheme" />
        <activity android:name=".accommodation.CampusesActivity" />
        <activity android:name=".airportpickup.ChatActivity" />
        <activity
            android:name=".calendar.CalendarActivity"
            android:label="@string/title_activity_calendar"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".aspire2info.AspireActivity"
            android:label="@string/title_activity_aspire"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CVWriterActivity" />
        <activity android:name=".VirtualTourActivity" />
        <activity android:name=".JobTools" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".accommodation.AucklandCampus" />
        <activity android:name=".accommodation.AucklandLocationActivity" />
        <activity android:name=".accommodation.AucklandFacilitiesActivity" />
        <activity android:name=".accommodation.TipAdviceActivity" />
        <activity android:name=".accommodation.ImportantContantActivity" />
        <activity android:name=".SplashActivityIntro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".accommodation.AucklandMapActivity"
            android:label="@string/title_activity_auckland_map" />
        <activity android:name=".airportpickup.ChatRoom" />
        <activity android:name=".airportpickup.SignInActivity" />
        <activity android:name=".airportpickup.UserLoginActivity" />
        <activity android:name=".airportpickup.ResetPasswordActivity" />
        <activity android:name=".airportpickup.MessagingActivity" />
        <activity android:name=".accommodation.NearestATM"></activity>
    </application>

</manifest>